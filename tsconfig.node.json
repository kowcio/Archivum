{
  "extends": "@tsconfig/node22/tsconfig.json",
  "include": [
    "wxt.config.*",
    "web-ext.config.*",
    "vite.config.*",
    "vitest.config.*",
    "nightwatch.conf.*",
    "playwright.config.*",
    "eslint.config.*",
    "src/test/playwright/**/*.ts",
    "src/test/playwright/**/*.js"
  ],
  "compilerOptions": {
    // Inherits from root: target, module, moduleResolution, noEmit,
    // esModuleInterop, forceConsistentCasingInFileNames, strict, skipLibCheck

    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",

    // Additional types for build tools
    "types": ["node", "@playwright/test", "wxt"],

    // Extra strict checks for build scripts
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  }
}

