/**
 * Store Module — Unified Backend Data Access Layer
 *
 * Single class combining storage definitions and all operations.
 * No instances needed — all methods are static.
 *
 * USAGE:
 * ✅ Backend: StorageRepository.getAppState()
 * ✅ Tests: StorageRepository.getAppState()
 * ✅ Frontend: import { useStore } from '@/composables/useStore'
 */

export { StorageRepository } from './StorageRepository'

